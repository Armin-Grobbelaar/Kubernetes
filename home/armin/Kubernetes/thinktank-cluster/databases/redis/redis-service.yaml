apiVersion: v1
kind: Service
metadata:
  name: redis-cluster
  namespace: databases
spec:
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: my-redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP